API (Controller): Занимается только HTTP (параметры, валидация, статус-коды).
Service: Содержит бизнес-логику (проверка "найден ли пользователь").
Repository: Знает, где лежат данные (в базе/файле/памяти) и как их достать.
Storage: Сами данные.
